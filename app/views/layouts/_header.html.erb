<nav class="navbar bg-light shadow">
  <div class="container-fluid d-flex justify-content-around p-3">
    <%= link_to 'Aura Notes', root_path, class:'navbar-brand fw-bold fs-2' %>
      <div  class="d-flex gap-4">
        <% if user_signed_in? %>
          <%= link_to 'Log out', destroy_user_session_path, data: {"turbo-method": :delete}, class:'border border-black rounded py-2 px-4 btn btn-light hover:bg-gray-200 fw-semibold' %>
        <% else %>
          <%= link_to 'Log in', new_user_session_path, class:'border border-black rounded py-2 px-4 btn btn-light hover:bg-gray-200 fw-semibold' %>
        <% end %>
      </div>  
  </div>
</nav>